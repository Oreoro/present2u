<% content_for :header do %>
  <%= leaf_nav_tag(leaf) do %>
    <%= link_to_previous_leafable(leaf, hotkey: false) %>

    <div class="breadcrumbs">
      <%= render "books/index_link" %>
      <span class="flex-item-no-shrink">▸</span>
      <%= link_to book.title, book_slug_path(book) %>
      <span class="flex-item-no-shrink">▸</span>
      <strong><%= leaf.leafable.title %></strong>
    </div>

    <button class="btn present-button" data-action="present#start">
      <%= image_tag "present.svg", aria: { hidden: true }, size: 24, class: "colorize--black" %>
      <span class="for-screen-reader">Present from this slide</span>
    </button>

    <button class="btn" data-action="present#fullscreenToggle">
      <%= image_tag "expand.svg", aria: { hidden: true }, size: 24 %>
      <span class="for-screen-reader">Toggle fullscreen (F)</span>
    </button>
  <% end %>
<% end %>

<% content_for :toolbar do %>
  <% if book.editable? %>
    <div class="page-toolbar fill-shade">
      <%= editing_mode_toggle_switch(@leaf, checked: false) %>
    </div>
  <% end %>
<% end %>
